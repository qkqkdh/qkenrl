import React from 'react';

import { Paper, Slide, Grid } from '@material-ui/core';

const ReviewModal = () => {
	const a = 1;
	return (
		<Grid>
			ReviewModal
		</Grid>
	);
};

export default ReviewModal;
