import React from 'react';

type Props = {
	places: any;
	setPlaces: any;
	handleClickPlaces?: any;
}

const PlaceSelector: React.FunctionComponent<Props> = (props) => (
	<div className="place__selector">
		test
	</div>
);

export default PlaceSelector;
